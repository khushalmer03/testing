name: b1 ONLY

on:
  push:
    branches:
      - b1

env:
  SOURCE_BRANCH: "${{ github.event.pull_request.head.ref }}"

jobs:

  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      timeout-minutes: 5
